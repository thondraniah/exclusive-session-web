{
  "name": "exclusive-session-members",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute exclusive-session-members --file=./schema.sql --local",
    "db:init:remote": "wrangler d1 execute exclusive-session-members --file=./schema.sql --remote"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
